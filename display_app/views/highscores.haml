!!!
%html
  %head
    %link{:rel => "stylesheet", :type => "text/css", :href => "/style.css"}
    %title= "Top 20 Players"

  %body{:onload => "timer=setTimeout('move()', 10000)"}
    %h1{:align => "center"}== Top 20 Players
    %table.display
      %thead
        %th Name
        %th C-Number
        %th Kills
        %th Deaths
        %th Medals

      - @players.each do |player|
        %tr{:class=>"player"}
          %td= "#{player[:first]} #{player[:last]}"
          %td= "#{player[:cnumber]}"
          %td= "#{player[:kills]}"
          %td= "#{player[:deaths]}"
          %td= "#{player[:medals]}"

%script{:type => "text/javascript"}
  :plain
    var timer = null
    function move() { window.location = '/highscores' }
